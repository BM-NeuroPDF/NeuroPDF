{
  "name": "neuropdf",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "description": "NeuroPDF monorepo — root tooling (Husky, lint-staged)",
  "scripts": {
    "prepare": "node -e \"if (process.env.CI === 'true') process.exit(0); const { spawnSync } = require('child_process'); const r = spawnSync('npx', ['husky'], { stdio: 'inherit', shell: true }); if (r.error) process.exit(1); process.exit(typeof r.status === 'number' ? r.status : 1);\"",
    "lint:staged": "lint-staged"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3",
    "prettier": "^3.5.3",
    "ruff": "^1.5.4"
  }
}
